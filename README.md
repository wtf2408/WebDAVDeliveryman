## 📦 WebDavDeliveryman (wddm)

**WebDavDeliveryman** — это консольная утилита для автоматической доставки файлов на WebDAV-сервер.<br>Позволяет загрузить целую папку, при этом учитывает список игнорируемых файлов и директорий, указанных в конфиге.

Идеально подходит для:
* резервного копирования
* синхронизации каталога с WebDAV-хранилищем

## 🚀 Быстрый запуск

### ⚙️ Сборка: 
1. Склонировать репозиторий:
```bash
git clone https://github.com/your-repo/WebDavDeliveryMan.git
cd WebDavDeliveryMan
```
2. Установить зависимости:
```bash
dotnet restore
```
3. Настроить config.json:
```json
{
  "url": "https://webdav.example.com/remote.php/webdav/",
  "username": "myUser",
  "password": "myPassword",

  "ignoreFolders": [".git", "bin", "obj"],
  "ignoreFiles": ["*.tmp", "*.bak"]
}
```
4. Собрать проект:
```bash
dotnet build
```
### 📄 Конфигурация
| Поле            | Тип      | Описание                                                 |
| --------------- | -------- | -------------------------------------------------------- |
| `url`           | string   | Адрес WebDAV сервера                                     |
| `username`      | string   | Логин для авторизации                                    |
| `password`      | string   | Пароль                                                   |
| `ignoreFolders` | string[] | Список папок, которые не нужно загружать                 |
| `ignoreFiles`   | string[] | Маски файлов, которые игнорируются (`*.tmp`, `*.log`, …) |

### 📁 Использование
```bash
wddm <путь_к_папке>
```
